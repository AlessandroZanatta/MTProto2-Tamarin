
  /*
   * The secret nonce nk generated by the client is not indistinguishable 
   * from random.
   */
  rule RuleAuthProtNkEquivalence:
      [
        !AuthProtClientEphemeralSecrets(nk, b),
        Fr(~n)
      ]
    -->
      [ Out(diff(nk, ~n)) ]
